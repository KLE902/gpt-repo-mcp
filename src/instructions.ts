export const SERVER_INSTRUCTIONS = [
  "GPT Repo MCP is a read-mostly local repository MCP app. Read tools inspect approved repositories. Mutating tools are disabled by default and require repo-local config opt-in.",
  "Mutating tools are repo_write_file, repo_write_changes, repo_write_handoff, repo_write_codex_task, repo_start_codex_task, repo_git_restore_paths, repo_write_stage, repo_write_unstage, repo_write_commit, repo_write_stage_commit, repo_write_recover, repo_cleanup_paths, repo_write_create_branch, repo_write_switch_branch, repo_write_push, repo_write_pull_request, repo_write_retire_pull_request, repo_write_retire_branch, repo_write_finalize_pull_request, repo_write_dispatch_workflow, repo_run_allowed_script, repo_write_sync_base, repo_write_update_branch_from_base, and repo_write_merge_pull_request, plus compatibility aliases repo_git_stage, repo_git_unstage, and repo_git_commit. Prefer the repo_write_* names for ChatGPT workflows except repo_git_restore_paths, which remains the granular worktree restore tool. For mutating workflows, inspect status, diff, or read context first and supply exact expected_head_sha or expected_staged_paths where required. A user request to implement or deliver authorizes reversible in-scope repository writes, creation of a new feature branch, local commits, feature-branch push, pull-request create/update, and guarded base or feature-branch synchronization; do not stop for a separate conversational approval between those steps. A client may still require a tool-call confirmation. Pull-request merge remains a separate explicit owner decision.",
  "repo_write_commit, repo_write_stage_commit, and repo_git_commit create local commits only. Local commit tools create commits only; remote work is exposed through separate fixed-purpose tools. Local commit tools do not push, pull, reset, checkout, switch, rebase, merge, stash, clean, force, delete branches, and do not run shell commands.",
  "For reviewed GitHub delivery from a base branch, use repo_write_create_branch to create and switch to a new feature branch from the exact current source branch and HEAD, then review and commit, use repo_write_push on the exact clean feature branch and HEAD, then repo_write_pull_request and repo_remote_status for pull-request and check state. Use repo_remote_pull_requests for bounded repository-wide PR audits. Use repo_branch_audit before standalone branch cleanup; it reports exact refs, ancestry, patch-equivalence, exact merged-PR evidence, ahead/behind counts, and open pull requests without mutation. Use repo_write_retire_branch only after the owner approves the exact branch and SHA and only when the audit provides strict containment evidence in the exact remote base with no open pull request. When the local policy configures the fixed github.pr-ready wrapper, run it through repo_run_allowed_script to delegate draft-ready to the authenticated GitHub CLI. Use repo_write_retire_pull_request only after the owner explicitly approves closing an exact open unmerged PR; it uses fixed GitHub CLI arguments and may clean only refs matching the approved PR head SHA. repo_write_create_branch never switches to an existing branch and may carry the already reviewed staged or unstaged worktree state. Remote operations are restricted to origin and GitHub.com. Force-push and direct push to main or master are blocked.",
  "Use repo_write_merge_pull_request only after the owner explicitly approves merging a specific pull request. Supply owner_approved true and the exact reviewed pull-request head SHA. Known checks must pass by default. After GitHub confirms the merge, prefer repo_write_finalize_pull_request to fast-forward the base, switch to it, and delete only the exact verified merged feature branch locally and optionally on origin. Use repo_write_sync_base when local base synchronization without switching or cleanup is needed. Use repo_write_update_branch_from_base only for a clean named feature branch, exact feature/base SHA guards, and conflict-free incorporation of the current origin base; conflicts are preflighted without entering merge state. GitHub API access comes only from GPT_REPO_GITHUB_TOKEN, GH_TOKEN, or GITHUB_TOKEN in the MCP server environment, while Git transport uses the host credential manager or SSH agent.",
  "When the user asks to create a ChatGPT handoff, says skapa handoff, create handoff, skriv handoff, session handoff, resume note, fortsättningsanteckning, ny chatt context, överlämning till nästa chatt, or similar private resume-context language, use repo_write_handoff instead of generic repo_write_file or repo_write_changes. Do not use repo_write_file or repo_write_changes for handoff workflows when repo_write_handoff exists. Before creating the handoff, run repo_git_status. If the worktree is dirty or changed files affect the next session, run repo_git_review and include relevant dirty-state, recovery notes, commit notes, or review warnings in the handoff current_state, risks, or next_steps.",
  "For repo_write_handoff, ChatGPT should summarize the session into structured fields: title, current_state, why, completed_work, decisions, workflow, constraints, next_steps, important_files, risks, and open_questions. The MCP server handles local-only handoff paths, .local.md enforcement, current.local.md updates, git metadata, file writing, write policy, and secret/content checks.",
  "Handoffs are local-only working context and normally should not be committed. If the user explicitly asks for public documentation, durable project knowledge, release notes, or audit notes, do not use the handoff workflow; use the normal docs/write workflow instead.",
  "Direct implementation with repo_write_file or repo_write_changes is the default when the user asks ChatGPT to fix, implement, update, or edit code. Use Codex task tools only when the user explicitly asks for a Codex prompt, Codex task, delegation to Codex, or a repo-local Codex run. For repo-local Codex delegation, use repo_write_codex_task first so the exact PROMPT.md and executable run.json exist before any start. When dedicated local Codex execution policy is enabled, use repo_start_codex_task only for that existing verified run with exact clean non-base branch and HEAD guards; the caller cannot provide prompt text, command, arguments, model, reasoning level, sandbox, timeout, environment, working directory, verification commands, or Git delivery instructions. Use repo_codex_review immediately or in a later turn to read durable starting, running, completed, blocked, failed, or timed-out state together with RESULT.md and Git review as applicable. Legacy manual RESULT.md runs remain supported. Use repo_prepare_codex_task only for chat-copy mode when no repo file should be written.",
  "For onboarding or daily planning, use repo_project_brief, then repo_task_inventory, then repo_next_action.",
  "Use repo_decision_memory for project memory, architecture decisions, conventions, patterns, and rationale. For implementation, refactor, or debug planning, use repo_decision_memory when conventions matter, then repo_change_plan, then targeted repo_search/repo_fetch_file/repo_read_many.",
  "Use repo_next_action to choose, prioritize, ship, or clean up next work. For current-change shipping, recovery, or commit prep, repo_git_review is the workflow hub: call it before commit/recovery decisions and use next_tool_payloads where possible instead of manually rebuilding path lists. Use repo_git_diff with only { repo_id } directly when the user only asks to inspect raw diff. Do not pass staged, unstaged, paths, max_bytes, or context_lines on the first direct diff call. Optional diff fields are second-pass refinements only when the default diff is truncated, too broad, or the user asks for a specific comparison.",
  "After repo_git_review, prefer composite workflow tools when payloads are available: use repo_write_stage_commit for reviewed happy-path local commits and repo_write_recover for reviewed recovery, cleanup, unstage, or restore. Use low-level tools only when the user asks for granular control, a composite payload is absent, staged-only commit requires repo_write_commit, a specific operation is requested, or troubleshooting/retry after composite failure.",
  "Dry-run is optional preview, not a required ritual. Use dry_run when the user asks for preview, risk is unclear, testing a new tool, or the state is unusual. For review-provided actual composite payloads inside an authorized workflow, call the actual composite tool directly.",
  "Omit optional reason by default to reduce payload friction. Use reason only when it adds meaningful audit context, keep it short, and if a client blocks a mutating call, retry without optional reason before changing the workflow.",
  "For continuity after an interruption or new turn, use repo_last_write to read safe receipt metadata, then repo_git_review for current git truth, then repo_write_stage_commit or repo_write_recover from review payloads.",
  "repo_write_recover is explicit path-only and can unstage, restore worktree paths, and clean configured artifacts; it does not reset, checkout, stash, clean, commit, push, or run shell commands.",
  "repo_run_allowed_script is not a shell. The caller supplies only a configured script_id and exact HEAD; the local config owns the executable, fixed arguments, timeout, output limit, and inherited environment names. Treat timeout, truncation, missing completion, or nonzero exit as a failed script result.",
  "Use repo_write_dispatch_workflow only for a locally allowlisted workflow_dispatch workflow, a validated remote branch, its exact expected SHA, and bounded string inputs. It runs on GitHub Actions, not automatically on the local PC.",
  "Use repo_plan_review before broad or ambiguous reviews. For code drilldown, use repo_tree for structure, repo_search for locating relevant code, repo_fetch_file for specific files, and repo_read_many only for bounded known sets.",
  "All paths are repo-relative POSIX paths and all repository access is scoped by repo_id.",
  "Default excludes and secret blocking are enforced by the server; do not ask for absolute paths or secrets.",
  "When a read, write, or cleanup policy question is blocked or the user asks what ChatGPT can access, call repo_policy_explain with the relevant repo_id and path before guessing. Use its code, matched_globs, effective_policy, local and remote operation toggles, and guidance to explain supported read/write/cleanup policy and enabled operation settings.",
  "Nested repositories and submodules are separate trust boundaries and are not read unless registered as their own repo_id."
].join(" ");
